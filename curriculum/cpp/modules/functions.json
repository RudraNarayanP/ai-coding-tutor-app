{
  "id": "cpp-functions",
  "title": "Unit 4: C++ Functions & Pass-By-Reference",
  "order": 4,
  "concepts": [
    {
      "id": "cpp-functions",
      "title": "Functions, Default Arguments & References",
      "prerequisites": ["cpp-loops"]
    }
  ],
  "lessons": [
    {
      "id": "cpp-functions-step-1",
      "title": "1. Pass-by-Reference (&)",
      "description": "In C++, pass-by-reference (`type&`) allows functions to modify caller variables efficiently. Implement `swap(int& a, int& b)`.",
      "order": 16,
      "difficulty": "intermediate",
      "duration_minutes": 5,
      "type": "learn",
      "concepts": ["cpp-functions"],
      "prerequisites": ["cpp-loops"],
      "learning_objectives": [
        "Use C++ reference parameters & to mutate values"
      ],
      "starter_code": "void swap_ints(int& a, int& b) {\n    // TODO: Swap values of a and b using references\n}\n",
      "solution_code": "void swap_ints(int& a, int& b) {\n    int temp = a;\n    a = b;\n    b = temp;\n}\n",
      "tests": [
        {
          "name": "test_swap",
          "test_code": "int x = 5, y = 10; swap_ints(x, y); assert(x == 10 && y == 5);",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": ["test_swap"]
      }
    },
    {
      "id": "cpp-functions-step-2",
      "title": "2. Const References (const &)",
      "description": "Pass large objects by `const &` to avoid expensive copies while guaranteeing immutability. Implement `greeting(const std::string& name)`.",
      "order": 17,
      "difficulty": "intermediate",
      "duration_minutes": 5,
      "type": "learn",
      "concepts": ["cpp-functions"],
      "prerequisites": ["cpp-loops"],
      "learning_objectives": [
        "Pass parameters by const std::string& in C++"
      ],
      "starter_code": "#include <string>\n\nstd::string greeting(const std::string& name) {\n    // TODO: Return \"Hello, \" + name + \"!\"\n    return \"\";\n}\n",
      "solution_code": "#include <string>\n\nstd::string greeting(const std::string& name) {\n    return \"Hello, \" + name + \"!\";\n}\n",
      "tests": [
        {
          "name": "test_greeting",
          "test_code": "assert(greeting(\"Ada\") == \"Hello, Ada!\");",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": ["test_greeting"]
      }
    },
    {
      "id": "cpp-functions-01",
      "title": "3. In-Place Range Clamping (Concept Challenge)",
      "description": "Clamp a double variable in-place using reference.",
      "order": 18,
      "difficulty": "intermediate",
      "duration_minutes": 10,
      "type": "challenge",
      "concepts": ["cpp-functions"],
      "prerequisites": ["cpp-loops"],
      "learning_objectives": [
        "Clamp values in-place using reference parameters"
      ],
      "starter_code": "void clamp(double& val, double min_val, double max_val) {\n    // TODO: If val < min_val, val = min_val; if val > max_val, val = max_val\n}\n",
      "solution_code": "void clamp(double& val, double min_val, double max_val) {\n    if (val < min_val) val = min_val;\n    else if (val > max_val) val = max_val;\n}\n",
      "tests": [
        {
          "name": "test_clamp",
          "test_code": "double v = 150.0; clamp(v, 0.0, 100.0); assert(v == 100.0); double v2 = -10.0; clamp(v2, 0.0, 100.0); assert(v2 == 0.0);",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": ["test_clamp"]
      }
    },
    {
      "id": "cpp-functions-practice-1",
      "title": "4. Value Incrementer (Practice)",
      "description": "Increment a reference value by amount.",
      "order": 19,
      "difficulty": "intermediate",
      "duration_minutes": 5,
      "type": "practice",
      "concepts": ["cpp-functions"],
      "prerequisites": ["cpp-loops"],
      "learning_objectives": [
        "Mutate reference parameter in C++"
      ],
      "starter_code": "void increment(int& value, int amount = 1) {\n    // TODO: Add amount to value\n}\n",
      "solution_code": "void increment(int& value, int amount = 1) {\n    value += amount;\n}\n",
      "tests": [
        {
          "name": "test_increment",
          "test_code": "int val = 10; increment(val, 5); assert(val == 15);",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": ["test_increment"]
      }
    },
    {
      "id": "cpp-functions-checkpoint",
      "title": "5. Unit 4 Checkpoint: C++ Functions Mastery",
      "description": "Verify function signature and reference skills.",
      "order": 20,
      "difficulty": "intermediate",
      "duration_minutes": 5,
      "type": "checkpoint",
      "concepts": ["cpp-functions"],
      "prerequisites": ["cpp-loops"],
      "learning_objectives": [
        "Pass C++ functions checkpoint"
      ],
      "starter_code": "int double_value(int n) {\n    // TODO: Return n * 2\n    return 0;\n}\n",
      "solution_code": "int double_value(int n) {\n    return n * 2;\n}\n",
      "tests": [
        {
          "name": "test_double_value",
          "test_code": "assert(double_value(21) == 42);",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": ["test_double_value"]
      }
    }
  ]
}
