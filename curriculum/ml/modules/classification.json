{
  "id": "classification",
  "title": "Unit 2: Classification",
  "order": 2,
  "concepts": [
    {
      "id": "classification",
      "title": "Classification",
      "prerequisites": [
        "linear-regression"
      ]
    }
  ],
  "lessons": [
    {
      "id": "clf-sigmoid",
      "title": "1. Sigmoid",
      "description": "Implement `sigmoid(z)` returning the logistic function 1 / (1 + e^-z) using math.exp. The tests check sigmoid(0) == 0.5 and sigmoid(10) > 0.99.",
      "order": 6,
      "difficulty": "intermediate",
      "duration_minutes": 5,
      "type": "learn",
      "section_id": "sec-ml-1",
      "section_title": "Section 1: ML Foundations",
      "concepts": [
        "classification"
      ],
      "prerequisites": [],
      "learning_objectives": [
        "Compute sigmoid probabilities"
      ],
      "starter_code": "import math\n\ndef sigmoid(z):\n    # TODO\n    pass\n",
      "solution_code": "import math\n\ndef sigmoid(z):\n    return 1 / (1 + math.exp(-z))\n",
      "tests": [
        {
          "name": "test_sigmoid",
          "unittest_code": "def test_sigmoid(self):\n    self.assertAlmostEqual(sigmoid(0), 0.5)\n    self.assertTrue(sigmoid(10) > 0.99)\n",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": [
          "test_sigmoid"
        ]
      },
      "source": {
        "name": "Google Machine Learning Crash Course — Glossary: 'logistic regression — a model that uses a logistic function to predict the probability of a binary outcome'",
        "url": "https://developers.google.com/machine-learning/glossary",
        "license": "CC BY 4.0 (Google Machine Learning Crash Course)"
      },
      "xp_reward": 10
    },
    {
      "id": "clf-logistic-predict",
      "title": "2. Logistic Predict",
      "description": "Implement `logistic_predict(x, w, b, threshold=0.5)` returning 0/1.",
      "order": 7,
      "difficulty": "intermediate",
      "duration_minutes": 5,
      "type": "learn",
      "section_id": "sec-ml-1",
      "section_title": "Section 1: ML Foundations",
      "concepts": [
        "classification"
      ],
      "prerequisites": [],
      "learning_objectives": [
        "Threshold logistic probabilities"
      ],
      "starter_code": "import math\n\ndef logistic_predict(x, w, b, threshold=0.5):\n    # TODO\n    pass\n",
      "solution_code": "import math\n\ndef logistic_predict(x, w, b, threshold=0.5):\n    p = 1 / (1 + math.exp(-(w * x + b)))\n    return 1 if p >= threshold else 0\n",
      "tests": [
        {
          "name": "test_logistic_predict",
          "unittest_code": "def test_logistic_predict(self):\n    self.assertEqual(logistic_predict(10, 1, 0), 1)\n    self.assertEqual(logistic_predict(-10, 1, 0), 0)\n",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": [
          "test_logistic_predict"
        ]
      },
      "source": {
        "name": "Google Machine Learning Crash Course — Glossary: logistic regression (probability of a binary outcome)",
        "url": "https://developers.google.com/machine-learning/glossary",
        "license": "CC BY 4.0 (Google Machine Learning Crash Course)"
      },
      "xp_reward": 10
    },
    {
      "id": "clf-knn",
      "title": "3. k-NN Classify",
      "description": "Implement `knn_classify(x, points, labels, k)` majority vote of k nearest (1D). Tie → smaller label.",
      "order": 8,
      "difficulty": "intermediate",
      "duration_minutes": 8,
      "type": "challenge",
      "section_id": "sec-ml-1",
      "section_title": "Section 1: ML Foundations",
      "concepts": [
        "classification"
      ],
      "prerequisites": [],
      "learning_objectives": [
        "Classify with k-nearest neighbors"
      ],
      "starter_code": "from collections import Counter\n\ndef knn_classify(x, points, labels, k):\n    # TODO\n    pass\n",
      "solution_code": "from collections import Counter\n\ndef knn_classify(x, points, labels, k):\n    dist = sorted(((abs(x - p), lab) for p, lab in zip(points, labels)), key=lambda t: (t[0], t[1]))\n    votes = [lab for _, lab in dist[:k]]\n    counts = Counter(votes)\n    best = max(counts.values())\n    winners = sorted(lab for lab, c in counts.items() if c == best)\n    return winners[0]\n",
      "tests": [
        {
          "name": "test_knn",
          "unittest_code": "def test_knn(self):\n    self.assertEqual(knn_classify(0, [-1, 1, 2], [0, 1, 1], 3), 1)\n    self.assertEqual(knn_classify(0, [-2, -1, 5], [0, 0, 1], 2), 0)\n",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": [
          "test_knn"
        ]
      },
      "source": {
        "name": "scikit-learn User Guide — Nearest Neighbors — 'Classification is computed from a simple majority vote of the nearest neighbors'",
        "url": "https://scikit-learn.org/stable/modules/neighbors.html",
        "license": "BSD 3-Clause (scikit-learn documentation)"
      },
      "xp_reward": 25,
      "sublessons": [
        {
          "id": "knn-rule-sub",
          "title": "Select what the rule guarantees",
          "order": 1,
          "exercises": [
            {
              "id": "knn-rule-multi-1",
              "type": "select_multiple",
              "question": "The lesson's classifier takes the majority vote of the `k` nearest labels in 1D and breaks a tie toward the smaller label. Select every statement that follows from that rule.",
              "options": [
                "with k = 1 the nearest label alone decides",
                "a 1-1 tie between two labels returns the smaller label",
                "distance is measured as the absolute difference from `x`",
                "increasing `k` can change the predicted label",
                "the prediction is always the label of the farthest point"
              ],
              "correct_answer": [
                "with k = 1 the nearest label alone decides",
                "a 1-1 tie between two labels returns the smaller label",
                "distance is measured as the absolute difference from `x`",
                "increasing `k` can change the predicted label"
              ],
              "explanation": "Voting is over the k nearest by absolute distance, and ties resolve downward; a larger k pulls more neighbours into the vote, so the winner can flip.",
              "xp_reward": 10
            }
          ]
        }
      ]
    },
    {
      "id": "clf-accuracy",
      "title": "4. Accuracy (Practice)",
      "description": "Implement `accuracy(y_true, y_pred)`: the fraction of positions where the prediction equals the label. The test checks accuracy([1, 0, 1], [1, 1, 1]) == 2/3.",
      "order": 9,
      "difficulty": "intermediate",
      "duration_minutes": 6,
      "type": "practice",
      "section_id": "sec-ml-1",
      "section_title": "Section 1: ML Foundations",
      "concepts": [
        "classification"
      ],
      "prerequisites": [],
      "learning_objectives": [
        "Compute classification accuracy"
      ],
      "starter_code": "def accuracy(y_true, y_pred):\n    # TODO\n    pass\n",
      "solution_code": "def accuracy(y_true, y_pred):\n    return sum(a == b for a, b in zip(y_true, y_pred)) / len(y_true)\n",
      "tests": [
        {
          "name": "test_accuracy",
          "unittest_code": "def test_accuracy(self):\n    self.assertAlmostEqual(accuracy([1, 0, 1], [1, 1, 1]), 2 / 3)\n",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": [
          "test_accuracy"
        ]
      },
      "source": {
        "name": "scikit-learn User Guide — 3.4 Metrics and scoring — classification metrics (accuracy)",
        "url": "https://scikit-learn.org/stable/modules/model_evaluation.html",
        "license": "BSD 3-Clause (scikit-learn documentation)"
      },
      "xp_reward": 15
    },
    {
      "id": "clf-checkpoint",
      "title": "5. Checkpoint: Precision & Recall",
      "description": "Implement `precision_recall(y_true, y_pred)` returning (precision, recall) for positive class 1. Use 0 if denom 0.",
      "order": 10,
      "difficulty": "intermediate",
      "duration_minutes": 10,
      "type": "checkpoint",
      "section_id": "sec-ml-1",
      "section_title": "Section 1: ML Foundations",
      "concepts": [
        "classification"
      ],
      "prerequisites": [],
      "learning_objectives": [
        "Compute precision and recall"
      ],
      "starter_code": "def precision_recall(y_true, y_pred):\n    # TODO\n    pass\n",
      "solution_code": "def precision_recall(y_true, y_pred):\n    tp = sum(yt == 1 and yp == 1 for yt, yp in zip(y_true, y_pred))\n    fp = sum(yt == 0 and yp == 1 for yt, yp in zip(y_true, y_pred))\n    fn = sum(yt == 1 and yp == 0 for yt, yp in zip(y_true, y_pred))\n    prec = tp / (tp + fp) if (tp + fp) else 0.0\n    rec = tp / (tp + fn) if (tp + fn) else 0.0\n    return prec, rec\n",
      "tests": [
        {
          "name": "test_precision_recall",
          "unittest_code": "def test_precision_recall(self):\n    p, r = precision_recall([1, 1, 0, 0], [1, 0, 1, 0])\n    self.assertAlmostEqual(p, 0.5)\n    self.assertAlmostEqual(r, 0.5)\n",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": [
          "test_precision_recall"
        ]
      },
      "source": {
        "name": "scikit-learn User Guide — 3.4 Metrics and scoring — precision, recall and f1-score",
        "url": "https://scikit-learn.org/stable/modules/model_evaluation.html",
        "license": "BSD 3-Clause (scikit-learn documentation)"
      },
      "xp_reward": 40
    }
  ]
}
