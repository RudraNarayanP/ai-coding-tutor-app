{
  "id": "conditionals",
  "title": "Unit 4: Conditionals",
  "order": 4,
  "concepts": [
    {
      "id": "conditionals",
      "title": "If, Elif, & Else Branching",
      "prerequisites": ["variables", "booleans"]
    }
  ],
  "lessons": [
    {
      "id": "conditionals-step-1",
      "title": "1. The `if` and `else` Statements",
      "description": "Conditional statements execute code blocks based on conditions. Use `if` and `else` to return \"Pass\" if grade >= 50, otherwise \"Fail\".",
      "order": 16,
      "difficulty": "beginner",
      "duration_minutes": 5,
      "type": "learn",
      "concepts": ["conditionals"],
      "prerequisites": ["variables", "booleans"],
      "learning_objectives": [
        "Use basic if-else branching"
      ],
      "starter_code": "def evaluate_grade(score):\n    # TODO: Return \"Pass\" if score >= 50 else \"Fail\"\n    pass\n",
      "tests": [
        {
          "name": "test_evaluate_grade",
          "unittest_code": "def test_evaluate_grade(self):\n    self.assertEqual(evaluate_grade(80), \"Pass\")\n    self.assertEqual(evaluate_grade(40), \"Fail\")",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": ["test_evaluate_grade"]
      }
    },
    {
      "id": "conditionals-step-2",
      "title": "2. Multi-way Branching with `elif`",
      "description": "When you have more than two branches, use `elif`. Implement `temperature_status(temp)` returning \"Cold\" (< 15), \"Warm\" (15..28), or \"Hot\" (> 28).",
      "order": 17,
      "difficulty": "beginner",
      "duration_minutes": 5,
      "type": "learn",
      "concepts": ["conditionals"],
      "prerequisites": ["variables", "booleans"],
      "learning_objectives": [
        "Chain conditions using elif"
      ],
      "starter_code": "def temperature_status(temp):\n    # TODO: Return \"Cold\" if temp < 15, \"Warm\" if temp <= 28, else \"Hot\"\n    pass\n",
      "tests": [
        {
          "name": "test_temperature_status",
          "unittest_code": "def test_temperature_status(self):\n    self.assertEqual(temperature_status(10), \"Cold\")\n    self.assertEqual(temperature_status(20), \"Warm\")\n    self.assertEqual(temperature_status(35), \"Hot\")",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": ["test_temperature_status"]
      }
    },
    {
      "id": "conditionals-01",
      "title": "3. Meltdown Mitigation (Concept Challenge)",
      "description": "Control nuclear reactor safety using multi-condition if/elif/else logic.",
      "order": 18,
      "difficulty": "beginner",
      "duration_minutes": 20,
      "type": "challenge",
      "concepts": ["conditionals"],
      "prerequisites": ["variables", "booleans"],
      "learning_objectives": [
        "Use if, elif, and else statements to control execution flow",
        "Evaluate numbers against threshold ranges",
        "Implement guard conditions and multi-clause boolean checks"
      ],
      "starter_code": "def is_criticality_balanced(temperature, neutrons_emitted):\n    \"\"\"Return True when reactor is balanced.\"\"\"\n    pass\n\ndef reactor_efficiency(voltage, current, theoretical_max_power):\n    \"\"\"Return efficiency band as a colour string.\"\"\"\n    pass\n\ndef fail_safe(temperature, neutrons_produced_per_second, threshold):\n    \"\"\"Return safety status string.\"\"\"\n    pass\n",
      "tests": [
        {
          "name": "test_is_criticality_balanced",
          "unittest_code": "def test_is_criticality_balanced(self):\n    self.assertEqual(is_criticality_balanced(750, 600), True)\n    self.assertEqual(is_criticality_balanced(800, 600), False)",
          "required": true
        },
        {
          "name": "test_reactor_efficiency_green",
          "unittest_code": "def test_reactor_efficiency_green(self):\n    self.assertEqual(reactor_efficiency(200, 50, 10000), 'green')",
          "required": true
        },
        {
          "name": "test_reactor_efficiency_orange",
          "unittest_code": "def test_reactor_efficiency_orange(self):\n    self.assertEqual(reactor_efficiency(150, 50, 10000), 'orange')",
          "required": true
        },
        {
          "name": "test_fail_safe_low",
          "unittest_code": "def test_fail_safe_low(self):\n    self.assertEqual(fail_safe(10, 100, 10000), 'LOW')",
          "required": true
        },
        {
          "name": "test_fail_safe_normal",
          "unittest_code": "def test_fail_safe_normal(self):\n    self.assertEqual(fail_safe(100, 100, 10000), 'NORMAL')",
          "required": true
        },
        {
          "name": "test_fail_safe_danger",
          "unittest_code": "def test_fail_safe_danger(self):\n    self.assertEqual(fail_safe(200, 100, 10000), 'DANGER')",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": [
          "test_is_criticality_balanced",
          "test_reactor_efficiency_green",
          "test_reactor_efficiency_orange",
          "test_fail_safe_low",
          "test_fail_safe_normal",
          "test_fail_safe_danger"
        ]
      },
      "source": {
        "name": "Exercism Python Track — Meltdown Mitigation",
        "url": "https://github.com/exercism/python/tree/main/exercises/concept/meltdown-mitigation",
        "license": "MIT"
      }
    },
    {
      "id": "conditionals-practice-1",
      "title": "4. Traffic Light System (Practice)",
      "description": "Implement `traffic_action(color)` returning \"Stop\" for Red, \"Prepare\" for Yellow, \"Go\" for Green.",
      "order": 19,
      "difficulty": "beginner",
      "duration_minutes": 5,
      "type": "practice",
      "concepts": ["conditionals"],
      "prerequisites": ["variables", "booleans"],
      "learning_objectives": [
        "Apply conditional branching to traffic state machines"
      ],
      "starter_code": "def traffic_action(color):\n    # TODO: Return \"Stop\" if \"red\", \"Prepare\" if \"yellow\", \"Go\" if \"green\"\n    pass\n",
      "tests": [
        {
          "name": "test_traffic_action",
          "unittest_code": "def test_traffic_action(self):\n    self.assertEqual(traffic_action(\"red\"), \"Stop\")\n    self.assertEqual(traffic_action(\"yellow\"), \"Prepare\")\n    self.assertEqual(traffic_action(\"green\"), \"Go\")",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": ["test_traffic_action"]
      }
    },
    {
      "id": "conditionals-checkpoint",
      "title": "5. Unit 4 Checkpoint: Conditionals Mastery",
      "description": "Demonstrate complete conditional flow control.",
      "order": 20,
      "difficulty": "beginner",
      "duration_minutes": 5,
      "type": "checkpoint",
      "concepts": ["conditionals"],
      "prerequisites": ["variables", "booleans"],
      "learning_objectives": [
        "Complete conditional flow evaluation"
      ],
      "starter_code": "def discount_rate(is_member, total_spent):\n    # TODO: Return 0.2 if is_member and total_spent >= 100 else 0.1 if is_member else 0.0\n    pass\n",
      "tests": [
        {
          "name": "test_discount_rate",
          "unittest_code": "def test_discount_rate(self):\n    self.assertEqual(discount_rate(True, 150), 0.2)\n    self.assertEqual(discount_rate(True, 50), 0.1)\n    self.assertEqual(discount_rate(False, 200), 0.0)",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": ["test_discount_rate"]
      }
    }
  ]
}
