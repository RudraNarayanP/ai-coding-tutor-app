{
  "id": "basics",
  "title": "Basics: Variables & Values",
  "order": 1,
  "concepts": [
    {
      "id": "variables",
      "title": "Variables & Simple Values",
      "prerequisites": []
    }
  ],
  "lessons": [
    {
      "id": "variables-01",
      "title": "Variable Assignment & Values",
      "description": "Learn how to store text and numbers in Python variables, reassign values, and combine variables with simple arithmetic.",
      "order": 1,
      "difficulty": "beginner",
      "duration_minutes": 10,
      "concepts": ["variables"],
      "prerequisites": [],
      "learning_objectives": [
        "Assign string and integer values to variables using =",
        "Create new variables using existing variables and simple arithmetic",
        "Understand Python variable naming conventions"
      ],
      "starter_code": "# Welcome to Python!\n# In Python, you store data in variables using the equals sign (=).\n# For example:\n# name = \"Alice\"\n# age = 20\n\n# TODO: Create a variable named `country` and set it to \"Italy\".\ncountry = None\n\n# TODO: Create a variable named `prep_time` and set it to 10.\nprep_time = None\n\n# TODO: Create a variable named `bake_time` and set it to 40.\nbake_time = None\n\n# TODO: Create a variable named `total_time` that equals prep_time + bake_time.\ntotal_time = None\n",
      "tests": [
        {
          "name": "test_country_variable",
          "unittest_code": "def test_country_variable(self):\n    failure_msg = 'Expected variable \"country\" to equal \"Italy\".'\n    self.assertEqual(country, \"Italy\", msg=failure_msg)",
          "required": true
        },
        {
          "name": "test_prep_time_variable",
          "unittest_code": "def test_prep_time_variable(self):\n    failure_msg = 'Expected variable \"prep_time\" to equal 10.'\n    self.assertEqual(prep_time, 10, msg=failure_msg)",
          "required": true
        },
        {
          "name": "test_bake_time_variable",
          "unittest_code": "def test_bake_time_variable(self):\n    failure_msg = 'Expected variable \"bake_time\" to equal 40.'\n    self.assertEqual(bake_time, 40, msg=failure_msg)",
          "required": true
        },
        {
          "name": "test_total_time_variable",
          "unittest_code": "def test_total_time_variable(self):\n    failure_msg = 'Expected variable \"total_time\" to equal prep_time + bake_time (50).'\n    self.assertEqual(total_time, 50, msg=failure_msg)",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": [
          "test_country_variable",
          "test_prep_time_variable",
          "test_bake_time_variable",
          "test_total_time_variable"
        ]
      },
      "source": {
        "name": "Patchwork Beginner Curriculum — Variable Assignment",
        "url": "",
        "license": "MIT"
      }
    }
  ]
}
