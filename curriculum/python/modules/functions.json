{
  "id": "functions",
  "title": "Unit 13: Functions & Constants",
  "order": 13,
  "concepts": [
    {
      "id": "functions",
      "title": "Functions, Parameters, & Docstrings",
      "prerequisites": ["variables", "numbers", "conditionals"]
    }
  ],
  "lessons": [
    {
      "id": "functions-step-1",
      "title": "1. Function Definition & Return Values",
      "description": "Functions encapsulate reusable code with `def name(param):` and return values with `return`.",
      "order": 61,
      "difficulty": "intermediate",
      "duration_minutes": 5,
      "type": "learn",
      "concepts": ["functions"],
      "prerequisites": ["variables", "numbers"],
      "learning_objectives": [
        "Define functions using def and return values"
      ],
      "starter_code": "def double(x):\n    # TODO: Return x * 2\n    pass\n",
      "tests": [
        {
          "name": "test_double",
          "unittest_code": "def test_double(self):\n    self.assertEqual(double(5), 10)",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": ["test_double"]
      }
    },
    {
      "id": "functions-step-2",
      "title": "2. Function Arguments & Docstrings",
      "description": "Functions accept parameters and document purpose using triple-quoted docstrings `\"\"\"Docstring...\"\"\"`.",
      "order": 62,
      "difficulty": "intermediate",
      "duration_minutes": 5,
      "type": "learn",
      "concepts": ["functions"],
      "prerequisites": ["variables", "numbers"],
      "learning_objectives": [
        "Pass multiple parameters and write function docstrings"
      ],
      "starter_code": "def area_of_rectangle(width, height):\n    \"\"\"Calculate rectangle area.\"\"\"\n    # TODO: Return width * height\n    pass\n",
      "tests": [
        {
          "name": "test_area_docstring",
          "unittest_code": "def test_area_docstring(self):\n    self.assertEqual(area_of_rectangle(4, 5), 20)\n    self.assertIsNotNone(area_of_rectangle.__doc__)",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": ["test_area_docstring"]
      }
    },
    {
      "id": "functions-01",
      "title": "3. Guido's Gorgeous Lasagna (Concept Challenge)",
      "description": "Bind names to values with constants, define functions using def and return, accept arguments with parameters, and document code with docstrings.",
      "order": 63,
      "difficulty": "intermediate",
      "duration_minutes": 25,
      "type": "challenge",
      "concepts": ["functions"],
      "prerequisites": ["variables", "numbers", "conditionals"],
      "learning_objectives": [
        "Define constants using SCREAMING_SNAKE_CASE",
        "Write functions with parameters, def, and return",
        "Add docstrings to every function"
      ],
      "starter_code": "\"\"\"Functions used in preparing Guido's gorgeous lasagna.\n\nLearn about Guido, the creator of the Python language:\nhttps://en.wikipedia.org/wiki/Guido_van_Rossum\n\nThis is a module docstring, used to describe the functionality\nof a module and its functions and/or classes.\n\"\"\"\n\n\n# TODO: define your EXPECTED_BAKE_TIME (required, set to 40) and PREPARATION_TIME (optional, set to 2) constants below.\nEXPECTED_BAKE_TIME = 40\nPREPARATION_TIME = 2\n\n\ndef bake_time_remaining(elapsed_bake_time):\n    \"\"\"Calculate the bake time remaining.\n\n    Parameters:\n        elapsed_bake_time (int): The baking time already elapsed.\n\n    Returns:\n        int: The remaining bake time (in minutes) derived from 'EXPECTED_BAKE_TIME'.\n\n    Function that takes the actual minutes the lasagna has been in the oven as\n    an argument and returns how many minutes the lasagna still needs to bake\n    based on the `EXPECTED_BAKE_TIME`.\n    \"\"\"\n    pass\n\n\n# TODO: Define the 'preparation_time_in_minutes(number_of_layers)' function below.\n\n\n# TODO: Define the 'elapsed_time_in_minutes(number_of_layers, elapsed_bake_time)' function below.\n\n\n# TODO: Remember to add docstrings to all your functions.\n",
      "tests": [
        {
          "name": "test_EXPECTED_BAKE_TIME",
          "unittest_code": "def test_EXPECTED_BAKE_TIME(self):\n    failure_msg = 'Expected a constant of EXPECTED_BAKE_TIME with a value of 40.'\n    self.assertEqual(EXPECTED_BAKE_TIME, 40, msg=failure_msg)",
          "required": true
        },
        {
          "name": "test_bake_time_remaining",
          "unittest_code": "def test_bake_time_remaining(self):\n    input_data = [1, 2, 5, 10, 15, 23, 33, 39]\n    result_data = [39, 38, 35, 30, 25, 17, 7, 1]\n\n    for variant, (time, expected) in enumerate(zip(input_data, result_data), start=1):\n        with self.subTest(f'variation #{variant}', time=time, expected=expected):\n            actual_result = bake_time_remaining(time)\n            failure_msg = (f'Called bake_time_remaining({time}). '\n                           f'The function returned {actual_result}, but the tests '\n                           f'expected {expected} as the remaining bake time.')\n\n            self.assertEqual(actual_result, expected, msg=failure_msg)",
          "required": true
        },
        {
          "name": "test_preparation_time_in_minutes",
          "unittest_code": "def test_preparation_time_in_minutes(self):\n    input_data = [1, 2, 5, 8, 11, 15]\n    result_data = [2, 4, 10, 16, 22, 30]\n\n    for variant, (layers, expected) in enumerate(zip(input_data, result_data), start=1):\n        with self.subTest(f'variation #{variant}', layers=layers, expected=expected):\n            actual_result = preparation_time_in_minutes(layers)\n            failure_msg = (f'Called preparation_time_in_minutes({layers}). '\n                           f'The function returned {actual_result}, but the tests '\n                           f'expected {expected} as the preparation time.')\n\n            self.assertEqual(actual_result, expected, msg=failure_msg)",
          "required": true
        },
        {
          "name": "test_elapsed_time_in_minutes",
          "unittest_code": "def test_elapsed_time_in_minutes(self):\n    layer_data = (1, 2, 5, 8, 11, 15)\n    time_data = (3, 7, 8, 4, 15, 20)\n    result_data = [5, 11, 18, 20, 37, 50]\n\n    for variant, (layers, time, expected) in enumerate(zip(layer_data, time_data, result_data), start=1):\n        with self.subTest(f'variation #{variant}', layers=layers, time=time, expected=expected):\n            actual_result = elapsed_time_in_minutes(layers, time)\n            failure_msg = (f'Called elapsed_time_in_minutes({layers}, {time}). '\n                           f'The function returned {actual_result}, but the tests '\n                           f'expected {expected} as the elapsed time.')\n\n            self.assertEqual(actual_result, expected, msg=failure_msg)",
          "required": true
        },
        {
          "name": "test_docstrings_were_written",
          "unittest_code": "def test_docstrings_were_written(self):\n    functions = [bake_time_remaining, preparation_time_in_minutes, elapsed_time_in_minutes]\n\n    for variant, function in enumerate(functions, start=1):\n        with self.subTest(f'variation #{variant}', function=function):\n            actual_result = function.__doc__\n            failure_msg = (f'Called {function.__name__}.__doc__. {actual_result} was returned, '\n                           f'but the tests expected a docstring for the {function.__name__} function.')\n\n            self.assertIsNotNone(actual_result, msg=failure_msg)",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": [
          "test_EXPECTED_BAKE_TIME",
          "test_bake_time_remaining",
          "test_preparation_time_in_minutes",
          "test_elapsed_time_in_minutes",
          "test_docstrings_were_written"
        ]
      },
      "source": {
        "name": "Exercism Python Track — Guido's Gorgeous Lasagna",
        "url": "https://github.com/exercism/python/tree/main/exercises/concept/guidos-gorgeous-lasagna",
        "license": "MIT"
      }
    },
    {
      "id": "functions-practice-1",
      "title": "4. Recipe Portions Calculator (Practice)",
      "description": "Calculate scaled recipe quantities for `servings` people.",
      "order": 64,
      "difficulty": "intermediate",
      "duration_minutes": 5,
      "type": "practice",
      "concepts": ["functions"],
      "prerequisites": ["variables", "numbers"],
      "learning_objectives": [
        "Define functions for recipe scaling"
      ],
      "starter_code": "def scale_recipe(base_amount, base_servings, target_servings):\n    \"\"\"Return scaled amount for target_servings.\"\"\"\n    # TODO: Return base_amount * (target_servings / base_servings)\n    pass\n",
      "tests": [
        {
          "name": "test_scale_recipe",
          "unittest_code": "def test_scale_recipe(self):\n    self.assertEqual(scale_recipe(200, 2, 4), 400.0)",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": ["test_scale_recipe"]
      }
    },
    {
      "id": "functions-checkpoint",
      "title": "5. Unit 13 Checkpoint: Functions Mastery",
      "description": "Complete the final curriculum checkpoint.",
      "order": 65,
      "difficulty": "intermediate",
      "duration_minutes": 5,
      "type": "checkpoint",
      "concepts": ["functions"],
      "prerequisites": ["variables", "numbers", "conditionals"],
      "learning_objectives": [
        "Master function definition and docstrings"
      ],
      "starter_code": "def is_even(n):\n    \"\"\"Return True if n is even.\"\"\"\n    # TODO: Return n % 2 == 0\n    pass\n",
      "tests": [
        {
          "name": "test_is_even",
          "unittest_code": "def test_is_even(self):\n    self.assertTrue(is_even(4))\n    self.assertFalse(is_even(5))",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": ["test_is_even"]
      }
    }
  ]
}
