{
  "id": "dictionaries",
  "title": "Unit 12: Dictionaries",
  "order": 12,
  "concepts": [
    {
      "id": "dictionaries",
      "title": "Dictionaries & Key-Value Mapping",
      "prerequisites": ["loops", "lists", "tuples"]
    }
  ],
  "lessons": [
    {
      "id": "dictionaries-step-1",
      "title": "1. Key-Value Lookup & Safe Retrieval (`.get()`)",
      "description": "Dictionaries store key-value pairs: `{\"apple\": 5, \"banana\": 3}`. Use `.get(key, default)` to safely retrieve values without raising KeyError.",
      "order": 56,
      "difficulty": "intermediate",
      "duration_minutes": 5,
      "type": "learn",
      "concepts": ["dictionaries"],
      "prerequisites": ["lists"],
      "learning_objectives": [
        "Access and assign dictionary keys and use .get()"
      ],
      "starter_code": "def get_item_count(inventory, item):\n    # TODO: Return inventory.get(item, 0)\n    pass\n",
      "tests": [
        {
          "name": "test_get_item_count",
          "unittest_code": "def test_get_item_count(self):\n    inv = {\"apple\": 10}\n    self.assertEqual(get_item_count(inv, \"apple\"), 10)\n    self.assertEqual(get_item_count(inv, \"orange\"), 0)",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": ["test_get_item_count"]
      }
    },
    {
      "id": "dictionaries-step-2",
      "title": "2. Iterating Over Dictionaries (`.items()`, `del`)",
      "description": "`dict.items()` yields `(key, value)` pairs during iteration. Use `del dict[key]` to delete a key.",
      "order": 57,
      "difficulty": "intermediate",
      "duration_minutes": 5,
      "type": "learn",
      "concepts": ["dictionaries"],
      "prerequisites": ["loops", "lists"],
      "learning_objectives": [
        "Iterate over key-value pairs with .items()"
      ],
      "starter_code": "def total_inventory_count(inventory):\n    # TODO: Return sum of all values in inventory using .items() or .values()\n    pass\n",
      "tests": [
        {
          "name": "test_total_count",
          "unittest_code": "def test_total_count(self):\n    self.assertEqual(total_inventory_count({\"a\": 5, \"b\": 15}), 20)",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": ["test_total_count"]
      }
    },
    {
      "id": "dictionaries-01",
      "title": "3. Inventory Management (Concept Challenge)",
      "description": "Create and mutate store inventory using dictionary methods.",
      "order": 58,
      "difficulty": "intermediate",
      "duration_minutes": 20,
      "type": "challenge",
      "concepts": ["dictionaries"],
      "prerequisites": ["loops", "lists", "tuples"],
      "learning_objectives": [
        "Create dictionaries from collections",
        "Use .get() and dict assignment to update counts",
        "Iterate over key-value pairs with .items()",
        "Delete keys safely"
      ],
      "starter_code": "def create_inventory(items):\n    \"\"\"Create inventory dict from item list.\"\"\"\n    pass\n\ndef add_items(inventory, items):\n    \"\"\"Increment or add items in inventory.\"\"\"\n    pass\n\ndef decrement_items(inventory, items):\n    \"\"\"Decrement counts, never below zero; ignore unknown items.\"\"\"\n    pass\n\ndef remove_item(inventory, item):\n    \"\"\"Remove item from inventory if present.\"\"\"\n    pass\n\ndef list_inventory(inventory):\n    \"\"\"Return list of (item, count) for items with count > 0.\"\"\"\n    pass\n",
      "tests": [
        {
          "name": "test_create_inventory",
          "unittest_code": "def test_create_inventory(self):\n    self.assertEqual(create_inventory(['coal', 'wood', 'wood']), {'coal': 1, 'wood': 2})",
          "required": true
        },
        {
          "name": "test_add_items",
          "unittest_code": "def test_add_items(self):\n    self.assertEqual(\n        add_items({'wood': 4, 'iron': 1}, ['wood', 'iron', 'diamond']),\n        {'wood': 5, 'iron': 2, 'diamond': 1}\n    )",
          "required": true
        },
        {
          "name": "test_decrement_items",
          "unittest_code": "def test_decrement_items(self):\n    self.assertEqual(\n        decrement_items({'wood': 2, 'iron': 1}, ['wood', 'wood', 'wood', 'iron']),\n        {'wood': 0, 'iron': 0}\n    )",
          "required": true
        },
        {
          "name": "test_remove_item",
          "unittest_code": "def test_remove_item(self):\n    self.assertEqual(\n        remove_item({'coal': 1, 'wood': 2}, 'coal'),\n        {'wood': 2}\n    )",
          "required": true
        },
        {
          "name": "test_list_inventory",
          "unittest_code": "def test_list_inventory(self):\n    self.assertEqual(\n        list_inventory({'coal': 7, 'wood': 11, 'diamond': 0}),\n        [('coal', 7), ('wood', 11)]\n    )",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": [
          "test_create_inventory",
          "test_add_items",
          "test_decrement_items",
          "test_remove_item",
          "test_list_inventory"
        ]
      },
      "source": {
        "name": "Exercism Python Track — Inventory Management",
        "url": "https://github.com/exercism/python/tree/main/exercises/concept/inventory-management",
        "license": "MIT"
      }
    },
    {
      "id": "dictionaries-practice-1",
      "title": "4. Word Frequency Counter (Practice)",
      "description": "Count word occurrences in a list using a dictionary.",
      "order": 59,
      "difficulty": "intermediate",
      "duration_minutes": 5,
      "type": "practice",
      "concepts": ["dictionaries"],
      "prerequisites": ["loops", "lists"],
      "learning_objectives": [
        "Aggregate frequencies with dictionaries"
      ],
      "starter_code": "def count_words(words):\n    # TODO: Return dict mapping word -> count\n    pass\n",
      "tests": [
        {
          "name": "test_count_words",
          "unittest_code": "def test_count_words(self):\n    self.assertEqual(count_words([\"a\", \"b\", \"a\"]), {\"a\": 2, \"b\": 1})",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": ["test_count_words"]
      }
    },
    {
      "id": "dictionaries-checkpoint",
      "title": "5. Unit 12 Checkpoint: Dictionaries Mastery",
      "description": "Verify dictionary mapping comprehension.",
      "order": 60,
      "difficulty": "intermediate",
      "duration_minutes": 5,
      "type": "checkpoint",
      "concepts": ["dictionaries"],
      "prerequisites": ["loops", "lists"],
      "learning_objectives": [
        "Master dictionary lookup and mutation"
      ],
      "starter_code": "def merge_inventories(inv1, inv2):\n    # TODO: Return new dict combining counts from inv1 and inv2\n    pass\n",
      "tests": [
        {
          "name": "test_merge_inventories",
          "unittest_code": "def test_merge_inventories(self):\n    res = merge_inventories({\"a\": 2}, {\"a\": 3, \"b\": 4})\n    self.assertEqual(res, {\"a\": 5, \"b\": 4})",
          "required": true
        }
      ],
      "completion_requirements": {
        "required_test_names": ["test_merge_inventories"]
      }
    }
  ]
}
