{
  "id": "sql-u8",
  "title": "Unit 8: JOINs",
  "order": 8,
  "concepts": [{ "id": "sql-joins", "title": "INNER and LEFT JOINs", "prerequisites": ["sql-group-by"] }],
  "lessons": [
    {
      "id": "sql-08",
      "title": "8. Relational JOINs",
      "description": "Join customers and orders on customers.id = orders.customer_id to select customer name and total_amount.",
      "order": 8,
      "difficulty": "intermediate",
      "duration_minutes": 10,
      "type": "learn",
      "section_id": "sec-sql-2",
      "section_title": "Section 2: Aggregation & Joins",
      "concepts": ["sql-joins"],
      "starter_code": "SELECT c.name, o.total_amount FROM customers c INNER JOIN orders o ON c.id = o.customer_id;\n",
      "solution_code": "SELECT c.name, o.total_amount FROM customers c INNER JOIN orders o ON c.id = o.customer_id;\n",
      "tests": [{ "name": "test_customer_orders_join", "test_code": "assert len(rows) >= 5 and columns[0] == 'name'", "required": true }],
      "completion_requirements": { "required_test_names": ["test_customer_orders_join"] }
    }
  ]
}
